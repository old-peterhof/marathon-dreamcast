# Dreamcast port — overnight report, 2026-09-06

Working copy: `~/Desktop/GPT Misc/MarathonDC`. Original: `~/Desktop/Marathon DC/port/AlephOne-0.12.0`.
Both are at the same commit and both are clean; the original was fast-forwarded
from the working copy. Either can be used from here.

## Status

b78 residency is built, committed, and soaked in Flycast. Not run on hardware.

| Image | For | Where |
|---|---|---|
| `alephone-b78-residency.cdi` (740 MB, padded, no markers, verified with `tools/verify-image.sh`) | **hardware** | both trees |
| `alephone-b78-residency-play.cdi` | Flycast, autostarts a level, DEBUG traces | both trees |
| `alephone-b78-residency-replay.cdi` | Flycast, plays the Waterloo film | working copy |
| `alephone-b78-residency-replay-l14.cdi` | Flycast, plays the level 14 film | working copy |

## Commits tonight

- `075a5e6` — Codex's VQ pack + eviction pass from 2026-09-03, committed as found (was uncommitted in both trees).
- `914255a` — b78: full-size textures throughout, real skies, compaction, the fixes, the deletions. The commit message is the detailed account; `README.DC.md` has a new section "Textures at full size".

## Review of the uncommitted work (what was deleted or simplified)

- **Deleted** `DC_ForceFullResolution` and its two call sites in `HUDRenderer_OGL.cpp` (about 50 lines). It exempted the HUD from a texture halving that no longer exists, and no Dreamcast UI can set Resolution to anything but 0.
- **Deleted** the PVR vertex-buffer half of `dc_memory_trace()`. GLdc submits through store queues, so `pvr_get_stats` reports 0 forever.
- **Simplified** the LRU's synchronisation and said what it actually guarantees: `pvr_wait_ready()` returns when the last scene has *started* rendering, so `pvr_wait_render_done()` is now called too before anything is deleted or compacted. Previously the compaction step could move a texture the PVR was still sampling (one glitched frame, not a crash).
- Hoisted the duplicated `Resolution = 0` in `OGL_Setup.cpp`; fixed the last `-fpermissive` warning in `OGL_Textures.cpp`.
- Kept: VQ pack, eviction + compaction, packed landscapes, lazy texture state, the replay target, the once-a-second memory trace. Each earns its place; see the README section.
- Net: 18 files, +219 / −233 lines for the whole b78 change.

## Evidence (Flycast, 16 MB, 200 MHz, FastGDRomLoad off)

Waterloo film, `/tmp/alephone-b78-run1.log`, about 10 minutes:

- VQ index loaded, 150+ compressed sprite uploads, **0 upload errors** (every slot-39 line reads `error=0000`).
- Eviction fired at 1028 KB free: 55 textures evicted, 1028 → 2475 KB. Next frame: 0 evicted, **compaction took the largest block 1016 → 2453 KB**. Game continued.
- Pause menu opened mid-level after real skies had loaded: `ui: upload 640x480 err=0x0, free 1827 KB, contiguous 1812 KB`. This is the allocation that killed b73.
- Screenshot at `scratchpad/map2.png` (session scratchpad): real purple sky, full-size walls, crisp Bob and magnum, sharp HUD.
- 30 fps throughout in Flycast. Heap top steady at 0x8ce33000; mallinfo live ~12.4 MB.
- The player dies at about tick 2700 in this film in every build (film/physics desync, pre-existing, not chased). The level reloads and the film keeps driving, so it still works as a soak.

## Open

1. **Hardware run.** Everything above is emulator evidence. The one assumption a console will test and Flycast cannot: a GD-ROM read the first time each sprite frame appears. Reads are sector aligned at both ends; a physical drive still seeks.
2. **Overhead map under full-size textures** was not exercised: film playback ignores the player's action flags, so it cannot be toggled during a replay. Needs a manual session (`-play` image, D-pad down).
3. **First level load is 26 s in Flycast** (`load: collections 25945 ms`, all in the bitmaps stage, 33,342 RLE rows). Codex's runs show the same on first load and 209 ms on a reload. The 704 ms figure in `11c39f3` was measured differently. Not a regression from tonight; worth one look with the load timers before it is called a hardware problem.
4. `GL ERROR: GL_INVALID_VALUE when calling glEnable` floods the DEBUG serial log. Pre-existing (see the 2026-09-02 handoff). Harmless but noisy; find the cap and remove the call.
5. Level 14 film run: in progress, see below.

## Level 14 soak

`alephone-b78-residency-replay-l14.cdi`, `/tmp/alephone-b78-l14.log`, about 15
minutes: one eviction pass (9 textures, 1028 -> 2394 KB), two level reloads
after the player died, 0 upload errors, no fatal. Stopped by hand to move on.

## Later in the night (in progress; each item is its own commit)

- **Level load: 26 s + 14 s -> under a second.** `d0f96f2`. KOS's ISO9660
  driver serves any read that is not sector-aligned into a 32-byte-aligned
  buffer one sector per GD-ROM command; stdio's 1 KB buffer guaranteed that.
  `dc_read_file_span()` reads a collection or a sound in one aligned transfer.
  Measured: collections 25945 -> 480 ms, monster sounds 13712 -> 10 ms.
- **Screen flashes were opaque white.** Root cause is in GLdc: with no colour
  array enabled, its generic `glDrawArrays` path fills every vertex white and
  ignores `glColor*`. Only the fast path (3-float positions) and immediate mode
  honoured the current colour. Aleph One's sprites and walls use GL_DOUBLE
  positions and the fader quad uses 2-float positions, so none hit the fast
  path: fades drew white and opaque, and sprites and unsplit walls drew unlit.
  Fixed in GLdc (`attributes.c`, `_fillCurrentColourARGB`), recorded as item 4
  in `tools/patches/gldc-1.1.1-fixes.patch`, backups in
  `/opt/toolchains/dc/kos-ports/libGL/.claude-backup-2026-09-06/`. libGL
  rebuilt (needs `source /opt/toolchains/dc/kos/environ.sh` first, or kos-cc
  fails with `exec: --: invalid option`).
- **Teleport static is real static now.** `TextureManager::StaticNoise`: for
  `_static_transfer` the sprite's texture is rebuilt every frame at quarter
  resolution with noise in the opaque texels, dissolving by `transfer_data`
  the way the desktop stipple did. The PowerVR has no logic ops or stipple, so
  the flicker lives in the texels. Verified visually with the STATICTEST hook
  (every sprite static): correct silhouettes of noise.
- **glEnable/glDisable filter** in `dc_gl_compat.h`: unknown capabilities are
  dropped and named once instead of leaving GL_INVALID_VALUE in glGetError
  every frame.
- Test hooks, emulator only: `FADETEST` (a different fade each second) and
  `STATICTEST` (everything teleports) markers, staged by
  `make replay-test FADETEST=1 STATICTEST=1 REPLAY_SUFFIX=-fx`.

## Queue from Max, overnight

1. RAM/VRAM crash safety, console optimisation, load times -- ongoing above.
2. Screen flashes: colours and duration -- root cause found and fixed, see
   above; duration was never wrong (250 ms for a hit), it read as long because
   it was opaque.
3. Teleport static -- done, see above.
4. **Saved-game features from the software fork**: saving at save terminals in
   the GL build opens the wrong menu or crashes. Not started.
5. **VMU saves as small as possible.** Currently deflate + fold against the
   level (163 blocks -> 22, per `dc_vmu.c`). Not re-measured tonight.
