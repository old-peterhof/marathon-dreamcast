/*
 *	dc_prefs.h -- Preferences as the prototype draws it. See dc_prefs.cpp.
 */

#ifndef DC_PREFS_H
#define DC_PREFS_H

#ifdef DC

/* The whole Preferences tree: root, Sound, Controls. Returns when the player
   backs out of the root. */
void dc_preferences(void);

#endif	/* DC */

#endif	/* DC_PREFS_H */
